import { ExpansiveButtonDirective } from './expansive-button.directive';

describe('ExpansiveButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpansiveButtonDirective();
    expect(directive).toBeTruthy();
  });
});
